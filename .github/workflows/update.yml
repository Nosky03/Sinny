name: Update Sinny
on:
  workflow_dispatch:
    inputs:
      file_content:
        description: 'Dati JSON'
        required: true

jobs:
  update-file:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Create index and index2 files
        run: |
          # Utilizziamo printf %s per iniettare il testo puro senza interpretare caratteri speciali
          printf "%s" "${{ fromJson(github.event.inputs.file_content).index_content }}" | base64 --decode > index.html
          printf "%s" "${{ fromJson(github.event.inputs.file_content).index2_content }}" | base64 --decode > index2.html

      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add index.html index2.html
          git commit -m "Update from Shortcut (using printf)"
          git push
